export type NavItem = {
  name: string;
  path: string;
};

export type Service = {
  title: string;
  description: string;
  path: string;
};

export type Socials = {
  icon: string;
  link: string;
};

export type Steps = {
  icon: string;
  step: string;
  description: string;
};

export type Achivements = {
  title: string;
  text: string;
};

export type Extras = {
  icon: string;
  title: string;
  text: string;
};

export type FooterLinks = {
  title: string;
  link: string;
};
